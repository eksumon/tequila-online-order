@extends('layouts.app')
@section('content')
<section id="about-page" class="mx-auto grid max-w-6xl gap-10 px-4 py-16 md:grid-cols-2"><div><p class="text-sm uppercase tracking-[0.25em] text-amber-300">Our story</p><h1 class="mt-2 font-serif text-5xl">About {{ $settings['business_name'] ?? 'Us' }}</h1><p class="mt-6 text-stone-300">{{ $settings['banner_desc'] ?? 'We serve fresh, flavorful dishes with warm hospitality and convenient online ordering.' }}</p><div class="mt-8 rounded-2xl border border-white/10 p-6"><h2 class="font-serif text-2xl">Visit us</h2><p class="mt-3 text-stone-400">{{ $settings['locations'] ?? 'Location coming soon.' }}</p><p class="mt-2 text-stone-400">{{ $settings['phone_no'] ?? '' }}</p></div></div><div class="rounded-2xl border border-white/10 bg-neutral-900/60 p-6"><h2 class="font-serif text-3xl">Opening Hours</h2><dl class="mt-6 space-y-3">@forelse($hours as $day=>$hour)<div class="flex justify-between border-b border-white/10 pb-3"><dt class="capitalize">{{ $day }}</dt><dd class="text-stone-300">{{ filter_var($hour['is_open'] ?? false, FILTER_VALIDATE_BOOLEAN) || ($hour['is_open'] ?? '') === '1' ? (($hour['opening'] ?? '') . ' - ' . ($hour['closing'] ?? '')) : 'Closed' }}</dd></div>@empty<p class="text-stone-400">Hours coming soon.</p>@endforelse</dl></div></section>
@endsection
